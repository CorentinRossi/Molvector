# Molvector

Molvector is an interactive 3D molecule viewer, builder, and Gaussian input generator.

You can install the software from pip:
```bash
pip install molvector
```
and update it to the last version:
```bash
pip install --upgrade molvector
```
inside the folder. Then, you can start the software with the command:
```bash
molvector
```